"""Seed for the Reef TTT demo task. See the task description in CORAL.md.

Implement ``merge_sorted(a, b)``: merge two ascending lists into one
ascending list, stable on ties (elements of ``a`` before ``b``), without
mutating the inputs and without calling ``sorted()``/``list.sort()``.
"""

from __future__ import annotations


def merge_sorted(a: list, b: list) -> list:
    raise NotImplementedError("merge two sorted lists")
