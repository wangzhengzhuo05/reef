[project]
name = "coral-demo-grader"
version = "0.0.1"
description = "Grader for the Reef test-time-training CORAL demo task"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
